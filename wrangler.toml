name = "painttothink"
compatibility_date = "2024-08-30"
pages_build_output_dir = "dist"

# KV namespace for site configuration
[[kv_namespaces]]
binding = "SITE_CONFIG"
id = "344662a60fe848c3bb6676e815d0efad"
preview_id = "c9933abd839846a883a9b2ebaba9afd3"

[env.production]
# Cloudflare Pages configuration for blazing speed

# KV namespace for production
[[env.production.kv_namespaces]]
binding = "SITE_CONFIG"
id = "344662a60fe848c3bb6676e815d0efad"

# Environment variables
[env.production.vars]
NODE_ENV = "production"
CONFIG_PASSWORD = "admin123"

# Note: Custom headers should be configured via _headers file in dist/ or public/
# Cloudflare Pages doesn't support rules in wrangler.toml for Pages projects
