# Cloudflare Pages SPA Redirects
# All routes serve index.html for client-side routing
# IMPORTANT: Do NOT catch /api/* - those are handled by Functions

# SPA routes - add new client routes here
/about /index.html 200
/services /index.html 200  
/contact /index.html 200

# Catch-all for SPA routes EXCEPT api and static assets
# Note: Functions and _routes.json handle /api/* paths
